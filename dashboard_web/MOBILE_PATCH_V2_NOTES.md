Dashboard Mobile Patch v2

Basis: main_mobile_v1.py / neue main(3).py

Geändert:
- Mobile Kopfbereich mit Logo/Ebolus/Gilden-Dashboard ist im normalen Mobile-Layout weg.
- Login-Zeile liegt mobil unten.
- Topnav-Buttons wie Status, Mein Portal, Loot, Events, Mitglieder liegen mobil ganz unten als horizontale Bottom-Bar.
- Menü-Button bleibt erreichbar und öffnet weiterhin das Vollbild-Menü.
- Hero/Wal-Bild ist mobil weiter nach rechts positioniert, damit der Wal sichtbarer ins Bild kommt.
- Zusätzlicher Bottom-Padding im Content, damit die unteren Bars nichts verdecken.
- Keine Bot-Logik, keine DB-Logik, keine Routen geändert.
